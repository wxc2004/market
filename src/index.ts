/**
 * =============================================================================
 * SkillMarket CLI 主入口文件
 * =============================================================================
 * 
 * 本文件是 SkillMarket CLI 工具的入口点。
 * 
 * 执行流程:
 * 1. Node.js 根据 shebang 启动
 * 2. 导入 cli.ts 中的命令行解析器
 * 3. Commander.js 解析 process.argv
 * 4. 调用对应的命令处理函数
 * 
 * @example
 * # 全局安装后直接使用
 * skm --help
 * 
 * # 使用 npx 运行
 * npx skillmarket --help
 * 
 * # 开发时运行
 * node dist/index.js --help
 */

// -----------------------------------------------------------------------------
// 导入 CLI 模块
// -----------------------------------------------------------------------------

/**
 * 导入 CLI 模块，启动命令行界面
 * 
 * cli.ts 使用 commander.js 配置所有命令选项，
 * 调用 program.parse() 开始解析命令行参数
 */
import './cli.js';
