/**
 * =============================================================================
 * SkillMarket - Electron Entry Point
 * =============================================================================
 *
 * 这个文件是 Electron 主进程的入口，导出 startGuiServer 供 electron/main.mjs 使用。
 * tsup 会单独编译这个文件为 electron-entry.js。
 * =============================================================================
 */

export { startGuiServer } from './commands/ui.js';
